import { Routes } from '@angular/router';
import { CalcFormComponent } from './calc-form.component';

export const CALC_FORM_ROUTES: Routes = [
  {
    path: '',
    component: CalcFormComponent,
  },
];

export default CALC_FORM_ROUTES;
